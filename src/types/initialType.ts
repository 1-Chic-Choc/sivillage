export interface quickMenuImageType {
  id: number;
  name: string;
  imgUrl: string;
  link: string;
  ctg_no: string;
}

export interface brandMenuType {
  id: number;
  name: string;
  link: string;
}

export interface quickMenuType {
  id: number;
  name: string;
  imgUrl: string;
  link: string;
}
